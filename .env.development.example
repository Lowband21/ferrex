# Ferrex Server development environment (example)

# Development posture
DEV_MODE=true

# Database (local dev)
DATABASE_URL=<REDACTED>

# Optional Redis for testing limiter locally
#REDIS_URL=redis://localhost:6379

# Server bind
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# Secrets (safe placeholders for local dev only)
AUTH_PASSWORD_PEPPER=<REDACTED>
AUTH_TOKEN_KEY=<REDACTED>

# TMDB key for metadata (optional in dev)
#TMDB_API_KEY=your_tmdb_api_key

# CORS in dev is permissive by default; override if needed
#CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

