###############################################
# Minimal Ferrex Server Environment (Example) #
###############################################

# --- Core services ---
# PostgreSQL connection string (required for sqlx)
DATABASE_URL=<REDACTED>
# Individual connection variables for server
PGHOST=/home/lowband/dev/ferrex-parent/ferrex-worktrees/dev/pgsock
PGDATABASE=ferrex
PGUSER=postgres
PGPASSWORD=your_password

# --- Server runtime ---
# Network bind (local development defaults)
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# Persistant logging level (optional)
# RUST_LOG=info

# --- Authentication secrets ---
# High-entropy secrets (set non-default values in any environment)
# AUTH_PASSWORD_PEPPER=changeme_super_secret_pepper_value
# AUTH_TOKEN_KEY=changeme_super_secret_hmac_key

# --- Online metadata ---
# TMDB key required for metadata lookups,
# scanning will fail if invalid or not present
# Can be freely created at https://developer.themoviedb.org/reference/getting-started
# TMDB_API_KEY=your_tmdb_api_key_here

# --- Optional TLS (commonly terminated by a reverse proxy) ---
#TLS_CERT_PATH=/certs/cert.pem
#TLS_KEY_PATH=/certs/key.pem

# --- Scanner config (optional) ---
#SCANNER_CONFIG_PATH=./config/scanner.toml
# or inline JSON overrides
#SCANNER_CONFIG_JSON={"quiescence_window_ms":5000}
