# Ferrex example environment configuration
#
# Note: `just start` generates secure credentials and writes a full config to `config/.env`.
# This example lists only a few core variables with placeholder values.

# Metadata (required)
TMDB_API_KEY=your_key_here

# Server bind address
FERREX_BIND=0.0.0.0:3000

# Database (local default)
DATABASE_URL=postgresql://postgres:password@localhost:5432/ferrex

# Redis (local default)
REDIS_URL=redis://localhost:6379

# TLS (optional: enable HTTPS directly in Ferrex)
# Point these to your certificate and key to run HTTPS without a reverse proxy.
#TLS_CERT_PATH=/path/to/cert.pem
#TLS_KEY_PATH=/path/to/key.pem

# Advanced TLS (optional)
# Minimum TLS version; defaults to 1.3
#TLS_MIN_VERSION=1.3

# Comma-separated TLS 1.3 cipher suites allow-list
#TLS_CIPHER_SUITES=TLS13_AES_256_GCM_SHA384,TLS13_CHACHA20_POLY1305_SHA256

# Security posture
# In non-dev, HTTPS is enforced by default. Set TRUST_PROXY_HEADERS=true when TLS
# terminates at a reverse proxy so the server respects X-Forwarded-Proto for redirects/HSTS.
#ENFORCE_HTTPS=true
#TRUST_PROXY_HEADERS=false
