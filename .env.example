# Environment variables for Rusty Media Server

# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=3000
RUST_LOG=debug

# Media configuration
MEDIA_ROOT=/path/to/your/media/files

# Metadata configuration (optional)
TMDB_API_KEY=<REDACTED>
CACHE_DIR=./cache

# FFmpeg configuration (auto-detected if not set)
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe