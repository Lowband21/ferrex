# Ferrex example environment configuration
#
# Note: `just start` generates secure credentials and writes a full config to `config/.env`.
# This example lists only a few core variables with placeholder values.

# Metadata (required)
TMDB_API_KEY=your_key_here

# Server bind address
FERREX_BIND=0.0.0.0:3000

# Database (local default)
DATABASE_URL=postgresql://postgres:password@localhost:5432/ferrex

# Redis (local default)
REDIS_URL=redis://localhost:6379
