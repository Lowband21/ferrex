# ferrexctl

`ferrexctl` is the configuration bootstrapper for the Ferrex project.

This crate provides both:
- a library (`ferrexctl`) with shared init/stack/config operations, and
- a CLI binary (`ferrexctl`).
