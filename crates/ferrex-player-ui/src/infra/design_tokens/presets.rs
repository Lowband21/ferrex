//! Named scale presets for common configurations.
//!
//! The settings domain owns the serializable preset DTO. The desktop UI turns
//! those presets into concrete scaling contexts at the rendering boundary.

pub use ferrex_player_settings::ScalePreset;
